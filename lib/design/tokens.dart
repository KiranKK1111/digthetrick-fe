/// Design tokens.
///
/// **All theme definitions now live in `lib/theme/theme.dart`** — this
/// file just re-exports so callers don't break. Edit theme.dart for
/// any palette / token change.
library;

export '../theme/theme.dart' show
    AppTheme,
    Palette,
    TypeScale,
    Spacing,
    Radii,
    DesignTokens;
